//file is used to append data to request
//this shows what the data should look like when sent to the backend
export interface AuthData{
  email: string;
  password: string;
  username: string;
  accountType: string;
}

